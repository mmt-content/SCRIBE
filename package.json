{
  "name": "hotel-research-tool",
  "private": true,
  "version": "0.1.0",
  "description": "Official-website-only hotel research tool powered by Gemini.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @hotel-research/web",
    "dev:api": "npm run dev --workspace @hotel-research/api",
    "build": "npm run build --workspace @hotel-research/core && npm run build --workspace @hotel-research/design && npm run build --workspace @hotel-research/api && npm run build --workspace @hotel-research/web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
